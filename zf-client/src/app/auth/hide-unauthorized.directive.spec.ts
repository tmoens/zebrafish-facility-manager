import {HideUnauthorizedDirective} from './hide-unauthorized.directive';

describe('HideUnauthorizedDirective', () => {
  it('should create an instance', () => {
    const directive = new HideUnauthorizedDirective();
    expect(directive).toBeTruthy();
  });
});
